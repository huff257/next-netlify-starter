export default function FlexSection({title, addClass}) {

    return (
      <section className={addClass}>
            <h2>{title}</h2>
            <div className="pc_row">
            
            </div>
        </section>
    )
}